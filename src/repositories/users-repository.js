import pkg from 'pg'
const { Client, Pool } = pkg;
export default class UserRepository {
    verify = async (token, secretKey) => {
        let respuesta;
        if (token =){

        }
        return respuesta
    }
}